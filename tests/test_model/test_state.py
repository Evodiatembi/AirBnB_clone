#!/usr/bin/python3
"""Defines unittests for models/state.py.
"""
import os
import models
import unittest
from models.state import State

clas TestState(unittest.Testcase):
    ste = state()
    self.assertIn(name, ste)

if __name__ == "__main__":
    unittest.main()
