#!/usr/bin/python3
"""Defines unittests for models/review.py"""
import os
import models
import unittest
from models.review import Review

class TestReview(unittest.TestCase):
    rw = Review()
    self.assertEqual(str, place_id)
    self.assertIn(place_id, rw)
    self.assertEqual(str, user_id)
    self.assertIn(user_id, rw)
    self.assertEqual(str, text)
    self.assertIn(text, rw)

if __name__ == "__main__":
    unittest.main()
