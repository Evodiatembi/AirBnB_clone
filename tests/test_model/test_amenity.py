#!/usr/bin/python3
"""Defines unittests for models/amenity.py."""
import os
import models
import unittest
from models.amenity import Amenity

class TestAmenity(unittest.TestCase):
     amt = Amenity()
     self.assertEqual(str, name)
     self.assertIn(name, Amt)
        
if __name__ == "__main__":
    unittest.main()
